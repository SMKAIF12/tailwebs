#HTML+CSS+BOOTSTRAP landing Page.
#Just open the index.html file in browser which is present in HTML+CSS folder

#React App(Search,Filter Feature using products)
#Install the dependencies using npm-install
#Implemented a dummy json server. So need to run the server in the terminal using the command
`npx json-server --watch data/data.json --port 3000 --static ./data`
#Run the react application using `npm run dev`
